describe('FeeRateProvider', () => {
  it('should fail due to high fee', () => {

  })
})

describe('PrevoutProvider', () => {
  it('should fail due to empty prevout in PrevoutProvider.all()', () => {

  })

  it('should fail due to empty prevout in PrevoutProvider.collect()', () => {

  })

  it('should fail balance not enough in PrevoutProvider.collect()', () => {

  })
})

describe('EllipticPairProvider', () => {
  it('should fail as provided EllipticPair cannot sign transaction', () => {

  })
})
