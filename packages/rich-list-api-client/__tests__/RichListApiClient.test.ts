// TODO: e2e, only able to fill up after rich-list-core implemented
describe('RichListApiClient - get', () => {
  beforeAll(async () => {
    /**
     * TODO:
     * 1. setup server env
     * 2. set RICH_LIST_LENGTH to maybe 5
     * 3. transact randomly among 10-20x addresses
     * 4. assert rich list
     * 5. repeat (3) to update rich list and (4)
     */
  })

  it('Should be able to retrieve latest rich list', async () => {
    // TODO(@ivan-zynesis):
  })
})
